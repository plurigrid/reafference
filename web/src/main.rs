fn main() {
    println!("reafference web member");
    println!();
    println!("Ratzilla-first browser shell for the Multiscale Health Innovation Network.");
    println!();
    println!("This crate keeps the same top-level `web` / `tui` split as nash-portal");
    println!("so `cargo build` works from repo root while the browser story stays");
    println!("explicitly aligned with the terminal story.");
    println!();
    println!("Browser-facing surfaces:");
    println!("  - web/index.html                 :: MHIN browser shell");
    println!("  - reafferance.health/index.html  :: byte-identical transport proof");
    println!("  - prototypes/ratzilla-surface    :: preferred browser thesis");
    println!("  - prototypes/restty-world        :: lower-level render backend");
}
